# SPEC ONLY until C1 is implemented and tested.
MODE: "stage_c_spec_only"
EXPERIMENT_NAME: "c_bn_bottleneck"

MODEL:
  TASK_NUISANCE:
    ENABLE: True
    VARIANT: "bottleneck"
    DEP_DIM: 96
    NUISANCE_DIM: 96
    RECONSTRUCTION_ENABLE: False
    CROSS_CORRELATION_ENABLE: False

LOSSES:
  RECONSTRUCTION_WEIGHT: 0.0
  CROSS_CORRELATION_WEIGHT: 0.0
