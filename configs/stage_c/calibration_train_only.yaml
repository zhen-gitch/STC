# Train-only raw auxiliary-loss calibration support run.
EXPERIMENT_NAME: "c_loss_calibration"

EARLY_STOPPING:
  ENABLE: False

MODEL:
  TASK_NUISANCE:
    ENABLE: True
    VARIANT: "split"
    DEP_DIM: 96
    NUISANCE_DIM: 96
    RECONSTRUCTION_ENABLE: True
    CROSS_CORRELATION_ENABLE: True
    AUXILIARY_CALIBRATION_ONLY: True
    CALIBRATION_STEPS: 100

LOSSES:
  RECONSTRUCTION_WEIGHT: 0.0
  CROSS_CORRELATION_WEIGHT: 0.0
